name: Ping event

on:
  push:
    branches:
    - 'release/2.x'
    - 'release/3.x'

jobs:
  echo:
    runs_on: ubuntu
    steps:
    - name: Echo Hello World
      run: |
        echo "Hello World at ${{ github.ref_name }}"

